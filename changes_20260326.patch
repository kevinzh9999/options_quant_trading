diff --git a/CLAUDE.md b/CLAUDE.md
index 5475a7c..ecd9731 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -57,8 +57,9 @@ A股股指期货/期权多策略量化交易系统（实盘运行中）。
 - 高波动区间阈值放宽：2-3%逆势 d=0.5（不是0.3）
 - 方向感知：顺势不打折或轻打折，逆势重打折
 
-### 平仓信号系统（2026-03-24新增）
-- 7个优先级：EOD_CLOSE > STOP_LOSS > TRAILING_STOP > TREND_COMPLETE > MOMENTUM_EXHAUSTED > MID_BREAK > TIME_STOP
+### 平仓信号系统（2026-03-24新增，03-26优先级调整）
+- 7个优先级：EOD_CLOSE > STOP_LOSS > LUNCH_CLOSE > TRAILING_STOP > TREND_COMPLETE > MOMENTUM_EXHAUSTED > MID_BREAK > TIME_STOP
+- STOP_LOSS 提升到 LUNCH_CLOSE 之前（防止午休前大亏不止损）
 - TREND_COMPLETE：5分钟上轨 + 15分钟上轨 = 两个周期都到极端才平仓
 - 用15分钟布林带判断趋势阶段，5分钟只用于跟踪止盈
 - 动态跟踪止盈：持仓时间越长宽度越大（0.5%→1.0%）
@@ -116,12 +117,13 @@ garch_reliable                                          ← GARCH可靠性（1=
 - 期货公司：宏源期货（broker="H宏源期货"）
 - 配置在 `.env` 文件中（TQ_ACCOUNT, TQ_PASSWORD, TQ_BROKER_ID, TQ_ACCOUNT_ID, TQ_BROKER_PASSWORD）
 
-## 当前持仓（需要定期更新）
-- MO2604-P-7200 空 4 手
-- MO2605-P-7200 空 2 手
-- MO2606-C-7800 多 10 手
-- MO2606-C-8400 空 12 手
-- 另有商品期权持仓（生猪、豆油）
+## 当前持仓（2026-03-26 收盘后更新）
+- MO2605-C-8600 空 3 手（卖Call端）
+- MO2605-C-8300 空 1 手（卖Call端）
+- MO2605-P-6800 空 3 手（卖Put端）
+- MO2605-P-6700 空 1 手（卖Put端）
+- MO2605-P-7500 多 1 手（保护性Put）
+- 账户权益约640万
 
 ---
 
diff --git a/scripts/backtest_signals_day.py b/scripts/backtest_signals_day.py
index b7ff851..0ca16d1 100644
--- a/scripts/backtest_signals_day.py
+++ b/scripts/backtest_signals_day.py
@@ -26,6 +26,7 @@ from data.storage.db_manager import DBManager
 from config.config_loader import ConfigLoader
 
 IM_MULT = 200  # IM合约乘数
+SLIPPAGE_PTS = 0  # 滑点（点），通过 --slippage 参数设置
 
 
 def _utc_to_bj(utc_str: str) -> str:
@@ -51,8 +52,13 @@ def _build_15m_from_5m(bar_5m: pd.DataFrame) -> pd.DataFrame:
     return resampled.reset_index(drop=True)
 
 
-def run_day(sym: str, td: str, db: DBManager, verbose: bool = True) -> List[Dict]:
-    """Replay one day. Returns list of completed trades."""
+def run_day(sym: str, td: str, db: DBManager, verbose: bool = True,
+            slippage: float = 0) -> List[Dict]:
+    """Replay one day. Returns list of completed trades.
+    
+    Args:
+        slippage: points of slippage per trade (applied adversely on entry and exit)
+    """
     date_dash = f"{td[:4]}-{td[4:6]}-{td[6:]}"
 
     # Load spot index 5min bars (preferred) or fallback to futures
@@ -228,10 +234,12 @@ def run_day(sym: str, td: str, db: DBManager, verbose: bool = True) -> List[Dict
                 exit_vol = exit_info["exit_volume"]
                 reason = exit_info["exit_reason"]
                 entry_p = position["entry_price"]
+                # Apply slippage adversely on exit
+                exit_p = price - slippage if position["direction"] == "LONG" else price + slippage
                 if position["direction"] == "LONG":
-                    pnl_pts = price - entry_p
+                    pnl_pts = exit_p - entry_p
                 else:
-                    pnl_pts = entry_p - price
+                    pnl_pts = entry_p - exit_p
 
                 elapsed = _calc_minutes(position["entry_time_utc"], utc_hm)
 
@@ -244,7 +252,7 @@ def run_day(sym: str, td: str, db: DBManager, verbose: bool = True) -> List[Dict
                         "entry_time": _utc_to_bj(position["entry_time_utc"]),
                         "entry_price": entry_p,
                         "exit_time": bj_time,
-                        "exit_price": price,
+                        "exit_price": exit_p,
                         "direction": position["direction"],
                         "pnl_pts": pnl_pts,
                         "reason": reason,
@@ -263,7 +271,7 @@ def run_day(sym: str, td: str, db: DBManager, verbose: bool = True) -> List[Dict
                         "entry_time": _utc_to_bj(position["entry_time_utc"]),
                         "entry_price": entry_p,
                         "exit_time": bj_time,
-                        "exit_price": price,
+                        "exit_price": exit_p,
                         "direction": position["direction"],
                         "pnl_pts": pnl_pts,
                         "reason": reason + "(半仓)",
@@ -279,9 +287,11 @@ def run_day(sym: str, td: str, db: DBManager, verbose: bool = True) -> List[Dict
                 in_cooldown = True
 
         if (position is None and not action_str and result and not in_cooldown
-                and score >= 55 and direction and is_open_allowed(utc_hm)):
+                and score >= _SIGNAL_THRESHOLD and direction and is_open_allowed(utc_hm)):
+            # Apply slippage adversely on entry
+            entry_p = price + slippage if direction == "LONG" else price - slippage
             position = {
-                "entry_price": price,
+                "entry_price": entry_p,
                 "direction": direction,
                 "entry_time_utc": utc_hm,
                 "highest_since": high,
@@ -290,8 +300,9 @@ def run_day(sym: str, td: str, db: DBManager, verbose: bool = True) -> List[Dict
                 "half_closed": False,
                 "bars_below_mid": 0,
             }
-            stop = price * (1 - STOP_LOSS_PCT) if direction == "LONG" else price * (1 + STOP_LOSS_PCT)
-            action_str = f"OPEN {direction} stop={stop:.0f}"
+            stop = entry_p * (1 - STOP_LOSS_PCT) if direction == "LONG" else entry_p * (1 + STOP_LOSS_PCT)
+            slip_tag = f" slip={slippage:.0f}" if slippage > 0 else ""
+            action_str = f"OPEN {direction} @{entry_p:.0f} stop={stop:.0f}{slip_tag}"
 
         # Build position status for display
         pos_info = ""
@@ -335,13 +346,15 @@ def run_day(sym: str, td: str, db: DBManager, verbose: bool = True) -> List[Dict
     if position is not None:
         last_price = float(all_bars.loc[today_indices[-1], "close"])
         entry_p = position["entry_price"]
-        pnl = (last_price - entry_p) if position["direction"] == "LONG" else (entry_p - last_price)
+        # Apply slippage adversely on force close
+        exit_p = last_price - slippage if position["direction"] == "LONG" else last_price + slippage
+        pnl = (exit_p - entry_p) if position["direction"] == "LONG" else (entry_p - exit_p)
         elapsed = _calc_minutes(position["entry_time_utc"], str(all_bars.loc[today_indices[-1], "datetime"])[11:16])
         completed_trades.append({
             "entry_time": _utc_to_bj(position["entry_time_utc"]),
             "entry_price": entry_p,
             "exit_time": _utc_to_bj(str(all_bars.loc[today_indices[-1], "datetime"])[11:16]),
-            "exit_price": last_price,
+            "exit_price": exit_p,
             "direction": position["direction"],
             "pnl_pts": pnl,
             "reason": "EOD_FORCE",
@@ -395,31 +408,158 @@ def _calc_minutes(t1: str, t2: str) -> int:
 def main():
     parser = argparse.ArgumentParser()
     parser.add_argument("--symbol", default="IM")
-    parser.add_argument("--date", default="20260324", help="YYYYMMDD or comma-separated")
+    parser.add_argument("--date", default="20260324", help="YYYYMMDD or comma-separated or YYYYMMDD-YYYYMMDD range")
+    parser.add_argument("--slippage", type=float, default=0, help="Slippage in points per trade (e.g. 5)")
+    parser.add_argument("--threshold", type=int, default=55, help="Signal threshold (default 55)")
+    parser.add_argument("--sensitivity", action="store_true",
+                        help="Run sensitivity sweep: slippage 0/5/10 × threshold 50/55/60")
     args = parser.parse_args()
 
     db = DBManager(ConfigLoader().get_db_path())
-    dates = [d.strip() for d in args.date.split(",")]
+
+    # Expand date range
+    if "-" in args.date and len(args.date) == 17:
+        # YYYYMMDD-YYYYMMDD range
+        start, end = args.date.split("-")
+        all_dates_df = db.query_df(
+            f"SELECT DISTINCT substr(datetime,1,10) as d FROM index_min "
+            f"WHERE symbol='000852' AND period=300 ORDER BY d"
+        )
+        if all_dates_df is not None and not all_dates_df.empty:
+            dates = [d.replace("-", "") for d in all_dates_df["d"].tolist()
+                     if start <= d.replace("-", "") <= end]
+        else:
+            dates = [start]
+    else:
+        dates = [d.strip() for d in args.date.split(",")]
+
+    if args.sensitivity:
+        _run_sensitivity(args.symbol, dates, db)
+        return
+
+    # Override threshold in signal generator
+    _patch_threshold(args.threshold)
 
     all_trades = []
     for td in dates:
-        trades = run_day(args.symbol, td, db)
+        trades = run_day(args.symbol, td, db, verbose=(len(dates) <= 3),
+                         slippage=args.slippage)
         all_trades.extend([(td, t) for t in trades])
 
     if len(dates) > 1:
-        print(f"\n{'='*60}")
-        print(f" Multi-Day Summary | {args.symbol}")
-        print(f"{'='*60}")
-        grand_pnl = 0
-        for td in dates:
-            day_trades = [t for d, t in all_trades if d == td and not t.get("partial")]
-            pnl = sum(t["pnl_pts"] for t in day_trades)
-            grand_pnl += pnl
-            n = len(day_trades)
+        _print_multi_day_summary(args.symbol, dates, all_trades, args.slippage, args.threshold)
+
+
+def _patch_threshold(threshold: int):
+    """Patch the signal threshold in SignalGeneratorV2 at module level."""
+    # The threshold is checked in run_day via `score >= 55`, we patch globally
+    global _SIGNAL_THRESHOLD
+    _SIGNAL_THRESHOLD = threshold
+
+
+# Module-level threshold (patched by _patch_threshold)
+_SIGNAL_THRESHOLD = 55
+
+
+def _run_sensitivity(sym: str, dates: List[str], db: DBManager):
+    """Run sensitivity sweep across slippage × threshold combinations."""
+    slippages = [0, 5, 10]
+    thresholds = [50, 55, 60]
+
+    print(f"\n{'='*80}")
+    print(f" SENSITIVITY ANALYSIS | {sym} | {len(dates)} days "
+          f"({dates[0]}~{dates[-1]})")
+    print(f"{'='*80}")
+
+    results = {}
+    for thr in thresholds:
+        _patch_threshold(thr)
+        for slip in slippages:
+            all_trades = []
+            for td in dates:
+                trades = run_day(sym, td, db, verbose=False, slippage=slip)
+                all_trades.extend(trades)
+            full_trades = [t for t in all_trades if not t.get("partial")]
+            total_pnl = sum(t["pnl_pts"] for t in full_trades)
+            n_trades = len(full_trades)
+            wins = len([t for t in full_trades if t["pnl_pts"] > 0])
+            win_days = len(set())  # placeholder
+            results[(thr, slip)] = {
+                "pnl": total_pnl, "n": n_trades,
+                "wr": wins / n_trades * 100 if n_trades > 0 else 0,
+                "yuan": total_pnl * IM_MULT,
+            }
+
+    # Print matrix
+    print(f"\n  PnL (points) | Slippage →")
+    print(f"  Threshold ↓  |  {'|  '.join(f'slip={s:2d}pt' for s in slippages)}  |")
+    print(f"  {'─'*14}+{'─'*42}")
+    for thr in thresholds:
+        row = []
+        for slip in slippages:
+            r = results[(thr, slip)]
+            pnl = r["pnl"]
+            row.append(f"{'+'if pnl>=0 else ''}{pnl:6.0f}pt({r['n']:2d}T)")
+        print(f"  thr={thr:3d}       | {'  | '.join(row)}  |")
+
+    print(f"\n  Yuan (1手IM) | Slippage →")
+    print(f"  Threshold ↓  |  {'|  '.join(f'slip={s:2d}pt' for s in slippages)}  |")
+    print(f"  {'─'*14}+{'─'*42}")
+    for thr in thresholds:
+        row = []
+        for slip in slippages:
+            r = results[(thr, slip)]
+            yuan = r["yuan"]
+            row.append(f"{'+'if yuan>=0 else ''}{yuan:8,.0f}")
+        print(f"  thr={thr:3d}       | {'  | '.join(row)}  |")
+
+    print(f"\n  WinRate      | Slippage →")
+    print(f"  Threshold ↓  |  {'|  '.join(f'slip={s:2d}pt' for s in slippages)}  |")
+    print(f"  {'─'*14}+{'─'*42}")
+    for thr in thresholds:
+        row = []
+        for slip in slippages:
+            r = results[(thr, slip)]
+            row.append(f"   {r['wr']:5.1f}%   ")
+        print(f"  thr={thr:3d}       | {'  | '.join(row)}  |")
+
+    # Robustness assessment
+    base = results[(55, 0)]["pnl"]
+    worst = results[(55, 10)]["pnl"]
+    if base != 0:
+        decay = (base - worst) / abs(base) * 100
+        print(f"\n  Robustness: base(thr55/slip0)={'+'if base>=0 else ''}{base:.0f}pt"
+              f" → worst(thr55/slip10)={'+'if worst>=0 else ''}{worst:.0f}pt"
+              f"  decay={decay:.1f}%")
+    print()
+
+
+def _print_multi_day_summary(sym: str, dates: List[str], all_trades: List,
+                              slippage: float, threshold: int):
+    """Print multi-day summary with slippage/threshold info."""
+    slip_tag = f" slip={slippage:.0f}pt" if slippage > 0 else ""
+    thr_tag = f" thr={threshold}" if threshold != 55 else ""
+    print(f"\n{'='*70}")
+    print(f" Multi-Day Summary | {sym}{slip_tag}{thr_tag} | {len(dates)} days")
+    print(f"{'='*70}")
+    grand_pnl = 0
+    win_days = 0
+    for td in dates:
+        day_trades = [t for d, t in all_trades if d == td and not t.get("partial")]
+        pnl = sum(t["pnl_pts"] for t in day_trades)
+        grand_pnl += pnl
+        if pnl > 0:
+            win_days += 1
+        n = len(day_trades)
+        if n > 0:
             print(f"  {td}: {n} trades  {'+'if pnl>=0 else ''}{pnl:.0f}pt")
-        yuan = grand_pnl * IM_MULT
-        print(f"\n  Grand Total: {'+'if grand_pnl>=0 else ''}{grand_pnl:.0f}pt"
-              f" = {'+'if yuan>=0 else ''}{yuan:,.0f}元")
+    yuan = grand_pnl * IM_MULT
+    total_days = len([td for td in dates
+                      if any(d == td for d, t in all_trades)])
+    wr_day = win_days / total_days * 100 if total_days > 0 else 0
+    print(f"\n  Grand Total: {'+'if grand_pnl>=0 else ''}{grand_pnl:.0f}pt"
+          f" = {'+'if yuan>=0 else ''}{yuan:,.0f}元(1手)"
+          f"  | {total_days}天 盈利{win_days}天({wr_day:.0f}%)")
 
 
 if __name__ == "__main__":
diff --git a/strategies/intraday/A_share_momentum_signal_v2.py b/strategies/intraday/A_share_momentum_signal_v2.py
index dbff6eb..c1ab109 100644
--- a/strategies/intraday/A_share_momentum_signal_v2.py
+++ b/strategies/intraday/A_share_momentum_signal_v2.py
@@ -199,7 +199,7 @@ def _calc_boll(closes: pd.Series, period: int = 20):
 
 def is_open_allowed(utc_hm: str) -> bool:
     """Check if opening new positions is allowed at this time."""
-    if utc_hm < "01:35":      # before 09:35 BJ
+    if utc_hm < "01:45":      # before 09:45 BJ (avoid opening noise)
         return False
     if NO_OPEN_LUNCH_START <= utc_hm < NO_OPEN_LUNCH_END:  # 11:20-13:05 BJ
         return False
@@ -220,7 +220,7 @@ def check_exit(
     """
     Multi-timeframe Bollinger exit system.
 
-    Priority: EOD > Lunch > StopLoss > TrailingStop > TrendComplete >
+    Priority: EOD > StopLoss > Lunch > TrailingStop > TrendComplete >
               MomentumExhausted > MidBreak > TimeStop
     """
     entry_price = position["entry_price"]
@@ -239,7 +239,16 @@ def check_exit(
         return {"should_exit": True, "exit_volume": volume,
                 "exit_reason": "EOD_CLOSE", "exit_urgency": "URGENT"}
 
-    # P1b: Lunch close (11:25 BJ)
+    # P1b: Stop loss (MUST be before lunch close — prevents large losses at lunch)
+    if direction == "LONG":
+        loss_pct = (entry_price - current_price) / entry_price
+    else:
+        loss_pct = (current_price - entry_price) / entry_price
+    if loss_pct > STOP_LOSS_PCT:
+        return {"should_exit": True, "exit_volume": volume,
+                "exit_reason": "STOP_LOSS", "exit_urgency": "URGENT"}
+
+    # P2: Lunch close (11:25 BJ)
     if current_time_utc >= LUNCH_CLOSE_UTC and current_time_utc < "05:00":
         if direction == "LONG":
             profitable = current_price > entry_price
@@ -260,15 +269,6 @@ def check_exit(
                 return {"should_exit": True, "exit_volume": volume,
                         "exit_reason": "LUNCH_TRAIL", "exit_urgency": "NORMAL"}
 
-    # P2: Stop loss
-    if direction == "LONG":
-        loss_pct = (entry_price - current_price) / entry_price
-    else:
-        loss_pct = (current_price - entry_price) / entry_price
-    if loss_pct > STOP_LOSS_PCT:
-        return {"should_exit": True, "exit_volume": volume,
-                "exit_reason": "STOP_LOSS", "exit_urgency": "URGENT"}
-
     # Compute Bollinger bands
     b5_mid, b5_std = float("nan"), float("nan")
     b15_mid, b15_std = float("nan"), float("nan")
